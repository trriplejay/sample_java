language: java

jdk:
   - oraclejdk8

build:
  ci:
    - echo $PATH
    - java -version
    - mvn --version
    - gradle --version
    - source shipctl jdk set oraclejdk11
    - echo $PATH
    - java -version
    - mvn --version
    - gradle --version
    #- mvn clean test

notifications:
   email: false
