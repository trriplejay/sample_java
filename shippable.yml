language: java

jdk:
   - oraclejdk8

build:
  ci:
    - java -version
    - mvn --version
    - gradle --version
    - source shipctl jdk set oraclejdk11
    - java -version
    - mvn --version
    - gradle --version
    #- mvn clean test

notifications:
   email: false
